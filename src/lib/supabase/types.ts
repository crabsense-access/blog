// Placeholder for generated Supabase types.
// Once the project is linked, generate real types with:
//   npx supabase gen types typescript --project-id <project-ref> --schema public > src/lib/supabase/types.ts
// Until then this keeps createClient/createServerClient typed loosely.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
